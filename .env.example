NODE_ENV=development
PORT=5000
# Optional: Leave empty to use auto-started in-memory MongoDB in dev
MONGODB_URI=
JWT_SECRET=supersecret_resumeiq_jwt_key_2026_production_grade
CLIENT_URL=http://localhost:5173
# Optional: Gemini API Key for AI resume enhancements (fallback heuristics provided if empty)
GEMINI_API_KEY=
AI_MODEL=gemini-1.5-flash
